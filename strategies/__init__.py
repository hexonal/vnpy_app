"""User CTA strategies loaded by vnpy_ctastrategy's CtaEngine.

CtaEngine.load_strategy_class() scans Path.cwd()/strategies as the module
`strategies` (in addition to the built-in vnpy_ctastrategy.strategies), so
run_gui.py — launched from this directory — picks up every strategy here.
"""
